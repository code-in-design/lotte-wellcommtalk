import styled from '@emotion/styled';
import Modal from 'react-modal';
import {commonModalStyles} from '@/styles/modal.styles';

export const WriteModalBlock = styled(Modal)`
  ${commonModalStyles};
  height: 684px;
`;
